# ==============================================
# VARIABLES DE ENTORNO PARA RAILWAY
# ==============================================

# ============== BACKEND (FastAPI) ==============
# Configuración básica
PYTHONPATH=/app
DEBUG=false
SECRET_KEY=your-secret-key-here

# Base de datos (si usas PostgreSQL de Railway)
DATABASE_URL=postgresql://user:password@host:port/database

# CORS Origins (URLs permitidas)
CORS_ORIGINS=["https://your-frontend-url.railway.app", "https://your-custom-domain.com"]

# ============== FRONTEND (Next.js) ==============
# Configuración de producción
NODE_ENV=production

# URL del backend (actualizar con tu URL real de Railway)
NEXT_PUBLIC_API_URL=https://your-backend-url.railway.app

# Supabase (si lo usas)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# ============== OPCIONALES ==============
# Logging
LOG_LEVEL=INFO

# Seguridad
ALLOWED_HOSTS=["your-backend-url.railway.app", "your-custom-domain.com"]

# Redis (si usas cache)
REDIS_URL=redis://user:password@host:port

# Email (si usas notificaciones)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-email-password

# ==============================================
# INSTRUCCIONES DE USO:
# ==============================================
# 1. Copia este archivo como .env.local para desarrollo
# 2. Configura estas variables en Railway Dashboard para producción
# 3. Nunca commitees archivos .env con valores reales
# 4. Usa NEXT_PUBLIC_ solo para variables que necesiten ser públicas
# 5. Actualiza las URLs con tus URLs reales de Railway 